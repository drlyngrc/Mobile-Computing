<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.LoginActivity" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/black</item>
        <item name="windowNoTitle">true</item> <!-- Ensures no title bar -->
        <item name="windowActionBar">false</item> <!-- Ensures no action bar -->
    </style>

    <style name="Theme.App" parent="Base.Theme.LoginActivity" />
</resources>